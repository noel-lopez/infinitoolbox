{
  "name": "@nlpz/utils",
  "version": "0.0.0",
  "description": "",
  "main": "./dist/index.ts",
  "types": "./dist/index.d.ts",
  "scripts": {
    "lint": "eslint \"**/*.ts*\"",
    "build": "tsup src/index.ts --format cjs --format cjs --dts",
    "dev": "pnpm run build -- --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "typescript": "^4.5.2",
    "tsup": "^7.1.0"
  },
  "author": "",
  "license": "MIT"
}
